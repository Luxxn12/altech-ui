import { ModalDemo, ModalPlayground } from "@/components/demos/overlay-demos"

export const metadata = {
  title: "Modal"
}

# Modal

Modal is built on accessible Radix Dialog primitives with smooth open/close transitions.

<ModalDemo />

## Interactive Playground

<ModalPlayground />

## Usage

```tsx
import {
  Modal,
  ModalTrigger,
  ModalContent,
  ModalHeader,
  ModalTitle,
  ModalDescription,
  ModalFooter,
} from "@altech-ui/react"
```
