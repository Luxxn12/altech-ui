import { ButtonDemo, ButtonPlayground } from "@/components/component-demos"

export const metadata = {
  title: "Button"
}

# Button

Buttons support variants, sizes, loading state, and micro-interactions.

<ButtonDemo />

## Interactive Playground

Try editing props below and the preview will update instantly:

<ButtonPlayground />

## Usage

```tsx
import { Button } from "@altech-ui/react"

<Button>Default</Button>
<Button variant="primary">Primary</Button>
<Button variant="secondary">Secondary</Button>
<Button variant="outline">Outline</Button>
<Button variant="ghost">Ghost</Button>
<Button variant="danger">Danger</Button>
<Button size="sm">Small</Button>
<Button size="md">Medium</Button>
<Button size="lg">Large</Button>
<Button isLoading>Loading</Button>
```

## Props

- `variant`: `default | primary | secondary | outline | ghost | danger`
- `size`: `sm | md | lg`
- `isLoading?`: `boolean`
- `disabled?`: `boolean`
- `className?`: `string`
- Native `button` props
