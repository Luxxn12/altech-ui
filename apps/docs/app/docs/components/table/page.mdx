import { TableDemo, TablePlayground } from "@/components/demos/table-demos"

export const metadata = {
  title: "Table"
}

# Table

Table primitives support semantic structure and loading skeleton state.

<TableDemo />

## Interactive Playground

<TablePlayground />

## Usage

```tsx
import {
  Table,
  TableHeader,
  TableBody,
  TableRow,
  TableHead,
  TableCell,
} from "@altech-ui/react"
```

Use `TableBody` with `isLoading` for loading placeholders.
