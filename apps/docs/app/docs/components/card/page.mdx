import { CardDemo, CardPlayground } from "@/components/component-demos"

export const metadata = {
  title: "Card"
}

# Card

Card is a layout surface with hover/press interactions and composition helpers.

<CardDemo />

## Interactive Playground

<CardPlayground />

## Usage

```tsx
import { Card, CardHeader, CardContent, CardFooter } from "@altech-ui/react"
```

Use `CardHeader`, `CardContent`, and `CardFooter` to keep structure consistent.
