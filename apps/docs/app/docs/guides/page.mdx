export const metadata = {
  title: "Guides"
}

# Guides

Additional best practices to help you build polished UI with Altech UI.

- Prefer semantic HTML and ARIA labels where needed.
- Keep animations short (150-250ms) and natural.
- Respect `prefers-reduced-motion` for accessibility.
- Compose primitives, avoid over-customizing one component for every case.
